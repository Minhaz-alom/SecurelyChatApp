[build]
  # Provide the exact folder that contains the HTML files
  publish = "src/main/resources/static"

[[redirects]]
  # Route all paths to index.html for Single Page Application routing flexibility
  from = "/*"
  to = "/index.html"
  status = 200
